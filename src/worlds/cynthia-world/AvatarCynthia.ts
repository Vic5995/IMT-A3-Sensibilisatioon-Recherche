class AvatarCynthia extends Avatar {
  pseudo: string;

  constructor(id: string, addressWorld: string, pseudo:string) {
    super(id, addressWorld);
    this.pseudo = pseudo;
  }

  // methods pour faire des trucs
}